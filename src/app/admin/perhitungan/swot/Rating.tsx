/** @format */

import React, { FC } from "react";

type Props = {
  dataTable: any;
};

const Rating: FC<Props> = ({ dataTable }) => {
  return (
    <>
      {/* internal */}
      <div></div>
      {/* external */}
      <div></div>
    </>
  );
};

export default Rating;
